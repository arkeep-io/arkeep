# .goreleaser.yml
#
# GoReleaser configuration for Arkeep.
# Triggered automatically by the release.yml GitHub Actions workflow on tag push.
#
# To test locally (dry run, no publish):
#   goreleaser release --snapshot --clean
#
# Reference: https://goreleaser.com/customization/

version: 2

project_name: arkeep

# ─── Build metadata ───────────────────────────────────────────────────────────

before:
  hooks:
    # Build the GUI and embed it into the server before compiling Go binaries.
    # The server embeds the GUI dist/ directory via go:embed at build time.
    - cmd: sh -c "cd gui && pnpm install --frozen-lockfile && pnpm build"
      output: true

    # Download restic and rclone binaries for all supported platforms.
    # These are embedded into the agent binary via go:embed.
    - cmd: sh -c "cd agent && go run ./scripts/download_deps.go --all-platforms"
      output: true

# ─── Binaries ─────────────────────────────────────────────────────────────────

builds:
  # Server — Linux and macOS only (no production use case for Windows server)
  - id: server
    binary: arkeep-server
    main: ./cmd/server
    dir: server
    env:
      - CGO_ENABLED=0
      - GOWORK=off
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.commit={{.Commit}}
      - -X main.date={{.Date}}

  # Agent — Linux, macOS, and Windows (agents run on any machine being backed up)
  - id: agent
    binary: arkeep-agent
    main: ./cmd/agent
    dir: agent
    env:
      - CGO_ENABLED=0
      - GOWORK=off
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    # Windows arm64 is excluded — restic does not ship a windows/arm64 binary,
    # so the embedded binary would be missing for that combination.
    ignore:
      - goos: windows
        goarch: arm64
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.commit={{.Commit}}
      - -X main.date={{.Date}}

# ─── Archives ─────────────────────────────────────────────────────────────────

archives:
  - id: server
    builds: [server]
    name_template: "arkeep-server_{{ .Os }}_{{ .Arch }}"
    format: tar.gz
    files:
      - LICENSE
      - README.md

  - id: agent
    builds: [agent]
    name_template: "arkeep-agent_{{ .Os }}_{{ .Arch }}"
    # Windows users expect .zip, everyone else gets .tar.gz
    format_overrides:
      - goos: windows
        format: zip
    files:
      - LICENSE
      - README.md
      - deploy/systemd/arkeep-agent.service

# ─── Checksums ────────────────────────────────────────────────────────────────

checksum:
  name_template: "checksums.txt"
  algorithm: sha256

# ─── Docker images ────────────────────────────────────────────────────────────

dockers:
  # Server — linux/amd64
  - id: server-amd64
    ids: [server]
    goos: linux
    goarch: amd64
    image_templates:
      - "ghcr.io/arkeep-io/arkeep-server:{{ .Tag }}-amd64"
      - "docker.io/arkeepio/arkeep-server:{{ .Tag }}-amd64"
    dockerfile: server/Dockerfile.goreleaser
    use: buildx
    build_flag_templates:
      - "--platform=linux/amd64"
      - "--label=org.opencontainers.image.title=arkeep-server"
      - "--label=org.opencontainers.image.version={{ .Version }}"
      - "--label=org.opencontainers.image.revision={{ .Commit }}"
      - "--label=org.opencontainers.image.created={{ .Date }}"
      - "--label=org.opencontainers.image.source=https://github.com/arkeep-io/arkeep"
      - "--label=org.opencontainers.image.licenses=AGPL-3.0"

  # Server — linux/arm64
  - id: server-arm64
    ids: [server]
    goos: linux
    goarch: arm64
    image_templates:
      - "ghcr.io/arkeep-io/arkeep-server:{{ .Tag }}-arm64"
      - "docker.io/arkeepio/arkeep-server:{{ .Tag }}-arm64"
    dockerfile: server/Dockerfile.goreleaser
    use: buildx
    build_flag_templates:
      - "--platform=linux/arm64"
      - "--label=org.opencontainers.image.title=arkeep-server"
      - "--label=org.opencontainers.image.version={{ .Version }}"
      - "--label=org.opencontainers.image.revision={{ .Commit }}"
      - "--label=org.opencontainers.image.created={{ .Date }}"
      - "--label=org.opencontainers.image.source=https://github.com/arkeep-io/arkeep"
      - "--label=org.opencontainers.image.licenses=AGPL-3.0"

  # Agent — linux/amd64
  - id: agent-amd64
    ids: [agent]
    goos: linux
    goarch: amd64
    image_templates:
      - "ghcr.io/arkeep-io/arkeep-agent:{{ .Tag }}-amd64"
      - "docker.io/arkeepio/arkeep-agent:{{ .Tag }}-amd64"
    dockerfile: agent/Dockerfile.goreleaser
    use: buildx
    build_flag_templates:
      - "--platform=linux/amd64"
      - "--label=org.opencontainers.image.title=arkeep-agent"
      - "--label=org.opencontainers.image.version={{ .Version }}"
      - "--label=org.opencontainers.image.revision={{ .Commit }}"
      - "--label=org.opencontainers.image.created={{ .Date }}"
      - "--label=org.opencontainers.image.source=https://github.com/arkeep-io/arkeep"
      - "--label=org.opencontainers.image.licenses=AGPL-3.0"

  # Agent — linux/arm64
  - id: agent-arm64
    ids: [agent]
    goos: linux
    goarch: arm64
    image_templates:
      - "ghcr.io/arkeep-io/arkeep-agent:{{ .Tag }}-arm64"
      - "docker.io/arkeepio/arkeep-agent:{{ .Tag }}-arm64"
    dockerfile: agent/Dockerfile.goreleaser
    use: buildx
    build_flag_templates:
      - "--platform=linux/arm64"
      - "--label=org.opencontainers.image.title=arkeep-agent"
      - "--label=org.opencontainers.image.version={{ .Version }}"
      - "--label=org.opencontainers.image.revision={{ .Commit }}"
      - "--label=org.opencontainers.image.created={{ .Date }}"
      - "--label=org.opencontainers.image.source=https://github.com/arkeep-io/arkeep"
      - "--label=org.opencontainers.image.licenses=AGPL-3.0"

# ─── Docker manifests (multi-arch) ────────────────────────────────────────────
# Combines the per-arch images into a single manifest list so users can pull
# without specifying the architecture suffix.

docker_manifests:
  - name_template: "ghcr.io/arkeep-io/arkeep-server:{{ .Tag }}"
    image_templates:
      - "ghcr.io/arkeep-io/arkeep-server:{{ .Tag }}-amd64"
      - "ghcr.io/arkeep-io/arkeep-server:{{ .Tag }}-arm64"

  - name_template: "ghcr.io/arkeep-io/arkeep-server:latest"
    image_templates:
      - "ghcr.io/arkeep-io/arkeep-server:{{ .Tag }}-amd64"
      - "ghcr.io/arkeep-io/arkeep-server:{{ .Tag }}-arm64"

  - name_template: "docker.io/arkeepio/arkeep-server:{{ .Tag }}"
    image_templates:
      - "docker.io/arkeepio/arkeep-server:{{ .Tag }}-amd64"
      - "docker.io/arkeepio/arkeep-server:{{ .Tag }}-arm64"

  - name_template: "docker.io/arkeepio/arkeep-server:latest"
    image_templates:
      - "docker.io/arkeepio/arkeep-server:{{ .Tag }}-amd64"
      - "docker.io/arkeepio/arkeep-server:{{ .Tag }}-arm64"

  - name_template: "ghcr.io/arkeep-io/arkeep-agent:{{ .Tag }}"
    image_templates:
      - "ghcr.io/arkeep-io/arkeep-agent:{{ .Tag }}-amd64"
      - "ghcr.io/arkeep-io/arkeep-agent:{{ .Tag }}-arm64"

  - name_template: "ghcr.io/arkeep-io/arkeep-agent:latest"
    image_templates:
      - "ghcr.io/arkeep-io/arkeep-agent:{{ .Tag }}-amd64"
      - "ghcr.io/arkeep-io/arkeep-agent:{{ .Tag }}-arm64"

  - name_template: "docker.io/arkeepio/arkeep-agent:{{ .Tag }}"
    image_templates:
      - "docker.io/arkeepio/arkeep-agent:{{ .Tag }}-amd64"
      - "docker.io/arkeepio/arkeep-agent:{{ .Tag }}-arm64"

  - name_template: "docker.io/arkeepio/arkeep-agent:latest"
    image_templates:
      - "docker.io/arkeepio/arkeep-agent:{{ .Tag }}-amd64"
      - "docker.io/arkeepio/arkeep-agent:{{ .Tag }}-arm64"

# ─── Release notes ────────────────────────────────────────────────────────────

changelog:
  use: github
  sort: asc
  groups:
    - title: "New Features"
      regexp: "^feat"
      order: 0
    - title: "Bug Fixes"
      regexp: "^fix"
      order: 1
    - title: "Performance Improvements"
      regexp: "^perf"
      order: 2
    - title: "Other Changes"
      order: 999
  filters:
    exclude:
      - "^docs:"
      - "^chore:"
      - "^test:"
      - "^refactor:"
      - "Merge pull request"
      - "Merge branch"

release:
  github:
    owner: arkeep-io
    name: arkeep
  # Mark pre-v1.0 releases as pre-release automatically
  prerelease: auto
  footer: |
    ## Docker Images

    ```bash
    # Server
    docker pull ghcr.io/arkeep-io/arkeep-server:{{ .Tag }}

    # Agent
    docker pull ghcr.io/arkeep-io/arkeep-agent:{{ .Tag }}
    ```

    ## Verify Checksums

    ```bash
    sha256sum --check checksums.txt
    ```