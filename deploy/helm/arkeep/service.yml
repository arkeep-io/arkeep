apiVersion: v1
kind: Service
metadata:
  name: {{ include "arkeep.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "arkeep.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "arkeep.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.httpPort }}
      targetPort: http
      protocol: TCP
    - name: grpc
      port: {{ .Values.service.grpcPort }}
      targetPort: grpc
      protocol: TCP